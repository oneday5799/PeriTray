> 本版为代码审查整改后的首个测试版：以**稳定性与正确性**为主，不引入新功能。

## 🐛 问题修复

- 修复「设置页保存会把弹窗里的改动一并抹掉」——例如在弹窗改完设备名，再到设置页切换任意开关，名字会被改回去
- 修复设置页正在编辑时收到后台配置更新，会**静默回滚**尚未保存的改动
- 修复配置文件里出现一个无法识别的值时**整份配置被重置为默认**（会连带丢失自定义设备名、窗口大小、开机自启等全部个性化设置）
- 修复升级后旧配置文件缺少新增字段时，同样会整份重置为默认
- 修复日志目录清理会误删用户自己放在该目录下的 `debug_*.log` 文件（删除不可逆且无任何提示）
- 修复启动第二个实例时会删掉正在运行实例的活动日志
- 修复低电量提醒「提醒过一次就再也不提醒」——现在充电后重新掉电可再次提醒
- 修复更新检查的版本号比较错误（如本地 1.2 与远端 1.2.0 被误判为有新版本，弹出装不上的更新提示）
- 修复快捷键被其他程序占用时界面显示「已设置」但按键毫无反应，且该键**静默永久失效**
- 修复音量等常规快捷键注册失败时完全不留日志、无从排查
- 修复托盘菜单切换默认音频设备失败时无任何提示（菜单已关闭，用户会误以为切换成功）
- 修复弹窗动画异常后**永久卡死**（再也打不开也关不掉，只能重启应用）
- 修复系统主题切换偶尔不生效（托盘图标停留在旧主题，需再切一次）
- 修复后台线程出错时弹出模态对话框，导致该线程卡住、应用整体失去响应
- 修复托盘音频菜单短暂显示过期的设备列表
- 修复通知文案里的换行标记被当作文本显示（如「发现新版本 x（当前 y）`<br>`点击前往下载」）
- 修复托盘设备数量上限在连续快速操作下可被突破
- 修复应用长时间运行后线程数持续增长（看门狗探活线程泄漏）
- 修复看门狗把「探活超时」误判为「系统从休眠唤醒」，而多做一次 WebView2 恢复
- 修复 WebView2 运行时就绪稍晚时前端整体失效（页面公共功能一次性全部消失）

## ⚡ 性能与流畅度

- **冷启动更快**：启动时创建应用快捷方式的操作移出启动关键路径（此前每次启动都要同步等待 0.3~1.5 秒）
- **改设置不再卡顿**：配置落盘改由专用写线程执行，不再占用界面线程
- **弹窗打开更快**：音频会话卡片先渲染、设备名随后回填，不再等设备名解析完才显示
- **蓝牙电量读取更快**：由「每台设备全量扫描一遍系统设备」改为一趟枚举
- 拖动音量滑块、切换音频设备更顺滑（消除持锁状态下做 COM 枚举）
- 打开日志目录 / 蓝牙设置 / 设备文件夹等操作不再冻结界面
- 系统通知弹出更快（通知图标不再每次都写一遍磁盘）

## 🧹 内部优化

- 全仓统一加锁入口，消除「锁异常后功能静默失效」这一类最难排查的哑故障
- 消除 2 处 AB/BA 死锁（曾表现为整进程随机冻结 10~20 秒后被系统判定「应用无响应」）
- 清理多处「持锁做 COM 调用 / 磁盘 I/O」的路径，避免界面被无关操作拖住
- 配置字段改为单点维护，杜绝「新增字段后设置页永远改不动」的漏项
- 收紧前端内容安全策略（CSP），并只允许通过白名单协议打开外部链接
- 固定 Rust 工具链版本，消除「本地绿、CI 红」的漂移
- 静态检查（clippy）存量告警从 20 类收敛到 1 类

**完整变更列表**：https://github.com/oneday5799/PeriTray/compare/v1.3.6...v1.3.7-beta.1
